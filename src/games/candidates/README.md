# Candidate games

Place an in-progress `<game>.game.ts` and `<game>.game.spec.ts` pair here.
Candidates participate in clean generation and compiler CI, but are excluded
from accepted real-ROM sweeps and the public catalog until promoted.
